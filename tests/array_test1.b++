// importar la libreria std
import "stdlib";

// crear array
Array.new(MyArray, num, 43, 24, 56);
// declarar el indice
index = 0;

for (#MyArray)
{
    Array.GetFrom(MyArray, index, el); Sys.Out el
    index += 1;
}
